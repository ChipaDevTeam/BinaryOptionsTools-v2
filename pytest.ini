[pytest]
testpaths = tests/python/core tests/python/pocketoption tests/python/tracing
asyncio_mode = auto
asyncio_default_fixture_loop_scope = module
timeout = 60
markers =
    integration: marks tests that make real network requests (deselect with -k "not integration")
