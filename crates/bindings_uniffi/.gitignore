/target

# Build artifacts - generated UniFFI bindings
/out/

# Binary / compiled files
*.dll
*.dylib
*.so

# NOTE: docs_json/ is intentionally tracked: the uniffi_doc proc macro reads
# these files at compile time, so the crate cannot build without them.