fn main() {
    // UDL scaffolding disabled — no .udl file in this crate.
    // Bindings are generated via the uniffi-bindgen binary.
    // uniffi::generate_scaffolding("src/binary_options_tools_uni.udl").unwrap();
}
